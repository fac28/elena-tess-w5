export const ShowScore = () => {
    // render a "new page" component with the final score

    // to find the final score :
    // userAnswers an array of length 10
    // we should make an array correctAnswers from question.sdffdsfdf
    // if userAnswers == question.correct_answer
    // results = 0;
    // result ++
  }

