import PropTypes from "prop-types";
import { decode } from "html-entities";

const ShowScore = () => {
  return (
    <div>
      <h1>Suuuup</h1>
    </div>
  );
};

ShowScore.propTypes = {

};

export default ShowScore;
